services:
  highroad-math-solution:
    build: .
    container_name: highroad-math-solution
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    volumes:
      - ./시험지:/app/시험지
      - ./작업 완료:/app/작업 완료
    restart: unless-stopped
