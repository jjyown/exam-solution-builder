# 하이로드 수학 해설 제작기 체크리스트

- 문서 기준일: 2026-04-30

## 이번 작업 사이클 체크
- [x] Cursor 고정 규칙 추가: 작업 전 `docs/plan.md`, `docs/context.md`, `docs/checklist.md` 선확인
- [x] Cursor 고정 규칙 추가: 작업 완료 시 docs 3종 동시 업데이트
- [x] 문제 추출 품질 사전검증 1차 적용(작은/납작한 크롭 차단, 재추출 안내)
- [x] 해설 포맷 검증 1차 적용(`[정답]`, `[해설]` 누락 시 자동 재생성 1회)
- [x] 해설 정합 검증 2차 적용(정답-본문 모순/형식 혼합 감지 후 재생성)
- [x] 교육과정 외 용어/기호 필터 1차 적용(로피탈/편미분/선형대수 등 감지 후 재생성)
- [x] 문제 추출 비전 사전검증 2차 적용(`/api/precheck-extraction`, 점수 기반 생성 차단)
- [x] 품질 경고 플래그 UI 적용(자동 보정 이슈를 화면에 표시)
- [x] 우측 저장 버튼 UX 개편(해설 생성 전 숨김, 생성 후 단계형 노출)
- [x] 좌측 UI 단순화(현재 단계 안내/고급 설정 접기/AI 원문 기본 비노출)
- [x] DOCX 레이아웃 개편(빠른정답/해설 분리 + 해설 2단 컬럼 + 수식 표기 단순화)
- [x] 원본 HML 기반 추가 생성(A안): 업로드 후 해설 append DOCX 생성 API/버튼 추가
- [x] 시험지 목록 새로고침 수동화(자동 타이머 제거, 버튼 클릭 시만 갱신)
- [x] HML 문항 추출 패턴 확장(`1)`, `1.`, `1번`) 및 문단/스크립트 경계 추출 보강
- [x] 시험지 목록/API 허용 확장자에 한글 문서(`.hml`, `.hwp`, `.hwpx`) 추가 및 한글 파일 선택 시 전용 안내 메시지 적용
- [x] 수학비서형 빠른정답 단계 로직 적용(전체 제공 검증, 부분 누락 보완 생성, 불일치 표기/통계 응답)
- [x] 수학비서 HML 추출 순서 보정(문단 순서 유지 + `AUTONUM` 문항 번호 반영)
- [x] 시험지 목록 HML 선택 동선 개편(이미지 단계 차단, HML 전용 전환, 파일 타입 배지/선택 HML 안내/검증 통계 메시지)
- [x] HML 파서 이중 전략/정답표 원천 강화(문단우선+대체추출, ENDNOTE 정답표 우선, 파싱 진단 지표 노출)
- [x] HML 품질 PASS 규칙 반영(최소 문항 수/커버리지/불일치율) 및 경고 사유 프론트 노출
- [x] HML 문항 노이즈 자동 보정(문단↔평문 교차 대체) 및 Gemini 실패 시 GPT 백업 생성 경로/진단 노출
- [x] 실패 패턴 재시도 정책 적용(노이즈 전용 생성 프로필, 모델/프롬프트 차등 재시도, 노이즈 문항 수 진단)
- [x] 로컬 `OPENAI_API_KEY` 설정 반영 및 빌드 재검증
- [x] 번호 기반 완화 추출(`relaxed`) 추가 및 실샘플 재스모크 수행
- [x] AUTONUM 블록 기반 구조 추출 시도 및 실샘플 재스모크(0건 확인, 추가 규칙 필요 상태 기록)
- [x] 전문가 토의 기반 상시 작업 규칙 추가 및 ENDNOTE 제외 본문 문단추출 완화 재검증
- [x] ENDNOTE 오매치 정규식 수정 및 fallback 커버리지 우선 선택 적용/재스모크
- [x] AI 문항 추출 fallback 추가 및 실샘플 재스모크(AI 추출 0건 확인)
- [x] 사용자 대표 요청 파일군 프로필(`core-request-set`) 적용 및 진단 항목에 프로필 노출
- [x] 전문가 상세 리뷰 기반 안정성 수정(정답 불일치 재시도 백업 누락 보완, quickAnswerMode 판정 정합화)
- [x] 원문 구조 디코더 2차 적용(정답표 번호 앵커 기반 세그먼트 추출: 평문 번호 분할 + 문단 AUTONUM 결합) 및 실샘플 재스모크 완료
- [x] 2차 재스모크 결과 기록(봉명고 샘플: `questionCount=4`, `coverageRatio=20%`, 저커버리지 경고 지속)
- [x] HML 수동 문항 선택 입력 추가(`1-30`, `1,3,5-8`) 및 API 수동 필터/응답 반영
- [x] 원문 구조 디코더 3차 1차분 적용(태그 계층 기반 번호-본문-보기 결합)
- [x] 3차 1차분 재스모크 결과 기록(봉명고 샘플: `questionCount=2`, `coverageRatio=10%`, 저커버리지 지속)
- [x] 원문 구조 디코더 3차 2차분 적용(`ENDNOTE` 보기 기반 복원 추출)
- [x] 3차 2차분 재스모크 완료(봉명고 `questionCount=20`, 해동고 `questionCount=11`, 둘 다 `coverageRatio=1.0`)
- [x] 복원 문항 프롬프트 가드레일 적용(누락 본문 추정 금지, 보기/수치 기반 풀이 지시)
- [x] 모델 백업 장애 가드 적용(Gemini/GPT 모두 실패해도 문항 단위 수동검토로 대체하고 전체 진행)
- [x] 대표 샘플 자동 스모크 스크립트 추가(`npm run smoke:hml`, 타임아웃 환경변수 지원)
- [x] 수동 영역지정 보강(구분선 모드에서 X/Y 좌표% 입력으로 수동 구분선 추가)
- [x] 자동 스모크 타임아웃 실패 분류 검증(`HML_SMOKE_TIMEOUT_MS=90000` 설정 시 4개 샘플 시간초과로 실패 리포트 출력 확인)
- [x] `smokeFast` 커버리지 판정 보정(처리 문항 수 기준) 적용
- [x] 샘플 선택 실행 지원(`HML_SMOKE_SAMPLES`) 추가 및 2개 샘플 PASS 검증(봉명고/해동고)
- [x] 영역지정 메인 전환 1차 반영(HML 실행 모드 `manual` 기본 + `auto_assist` 보조, UI/응답 가이드 연동)
- [x] 운영 스모크 모드 분리(`HML_SMOKE_MODE`) 반영
- [x] 수동 모드 smokeFast 단일 샘플 검증 수행(봉명고: 문항수 부족으로 FAIL, 수동 우선 정책과 일치)
- [x] 빠른정답/해설참고 페이지 지정 기능 추가(PDF, 페이지 지정 입력/필수 작업 제외/생성 힌트 전달)
- [x] 페이지 지정 UX 보강(`빠른정답 없음`/`해설참고 없음` 체크박스, 체크 시 입력 비활성/검증 제외)
- [x] 수동 지정/대기열 표기 통일(`영역 박스`/`그림` -> `문제 박스`/`문제 n`)
- [x] 작업 완료 폴더 수동 저장 버튼 제거(PDF 저장 버튼만 유지)
- [x] 빠른정답/해설 참고 페이지 미지정 정상 처리 + 잘못된 페이지 지정 체크 경고 추가
- [x] 구분선/세로선 메인 흐름 제거 및 영역 박스 지정 메인 전환
- [x] Drive 연동 방식을 서비스 계정 키 -> OAuth 토큰 방식으로 전환
- [x] Vercel 런타임 저장 오류(`ENOENT`, `body.pipe`) 원인 분류 및 코드 수정
- [x] 저장 API(`/api/save-result`)의 Drive 업로드 경로 정상화
- [x] 해설 생성 시스템 프롬프트를 신규 양식(`[정답]`, `[해설]`)으로 교체
- [x] 프론트 파서를 신규 양식 우선 인식 + 기존 양식 하위호환 유지
- [x] DOCX 출력 포맷을 섹션형 문서 구조로 개선
- [x] API 에러 메시지 상세화(운영 로그 추적성 강화)
- [x] `npm run build`로 타입/빌드 검증 완료

## 배포 전 체크
- [ ] `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_REFRESH_TOKEN` 동일 OAuth 세트 확인
- [ ] `GEMINI_API_KEY` 설정 확인
- [ ] Drive 폴더 구조(`해설제작/시험지`, `해설제작/작업완료`) 존재 확인
- [ ] 민감정보 재발급/키 회전 여부 확인(노출 이력 대응)
- [ ] Git 커밋/푸시 후 Vercel Redeploy

## 배포 후 스모크 테스트
- [ ] 시험지 목록 새로고침(`/api/exams`) 성공
- [ ] 시험지 파일 로드(`/api/exams/file`) 성공
- [ ] 해설 생성(`/api/generate-explanation`) 성공
- [ ] 저장(`/api/save-result`) 성공 및 Drive `작업완료`에 DOCX 생성 확인
- [ ] 결과 DOCX가 `[정답]`, `[해설]` 중심 양식으로 출력되는지 확인

## 장애 원인분류 기록(요약)
- [x] `invalid_grant`: OAuth 토큰 세트 불일치/만료 이슈
- [x] `ENOENT /var/task/작업 완료`: Drive 모드에서도 로컬 mkdir 수행하던 로직 이슈
- [x] `body.pipe is not a function`: Drive 업로드 `media.body` 타입 이슈(Buffer 직접 전달)

## 다음 개선 후보
- [ ] 문제 추출 품질 사전검증 고도화(OCR/비전 기반 선택지·조건 누락 감지)
- [ ] 원문 구조 디코더 3차 적용(문항 번호-본문-보기 결합, 태그 계층 기반 경계 복원)
- [ ] 원문 구조 디코더 3차 2차분 적용(원문 태그 구조 직접 디코딩 + 번호/보기 병합)
- [ ] 원문 구조 디코더 품질 보강(보기만 복원된 문항의 해설 신뢰도 가드레일)
- [ ] 교육과정 범위 통제 고도화(금지어 사전 확대, 감지 결과 UI 경고/교체 제안)
- [ ] 운영 스모크 자동화(사전검증+생성 연속 케이스 PASS 기준 문서화)
- [ ] 자동 스모크 기준치 확정(샘플별 허용 시간/문항수/커버리지 PASS 컷 정의)
- [ ] 장시간 샘플(보인고/부산중앙여고) `smokeFast` 기준 타임아웃 원인 분리
- [ ] 자동보조 모드 단일/다중 샘플 결과를 최종 수치로 고정(현재 실행 중 세션 결과 포함)
