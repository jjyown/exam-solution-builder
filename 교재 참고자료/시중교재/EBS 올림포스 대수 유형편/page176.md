---
book: EBS 올림포스 대수 유형편
page: 176
ocrModel: gemini-2.0-flash
unit: 시중교재
type: EBS 올림포스 대수 유형편
difficulty: 미분류난이도
sourceImage: EBS 올림포스 대수 유형편/pages/page176.png
---

## OCR_본문

단계
채점 기준
0 주어진 두 곡선을 좌표평면 위에 나타낸 경우
비율
40%
$f(2) = \log_2 2 + 4 = 1 + 4 = 5$
$f(3) = \log_2 1 + 4 = 0 + 4 = 4$
2 주어진 두 곡선이 서로 다른 두 점에서 만나고, 만나
는 두 점의 $x$좌표의 곱이 음수일 조건을 구한 경우
함수 $y = f(x)$의 그래프와 $x$축 및 $y$축으로 둘러싸인 도형과 그 내부에
40%
있는 점 중 $x$좌표와 $y$좌표가 모두 자연수인 점은
3 자연수 $n$의 값의 합을 구한 경우
20%
$x$좌표가 1일 때, $y$좌표는 1, 2, 3, 4, 5
$x$좌표가 2일 때, $y$좌표는 1, 2, 3, 4, 5
02 함수 $f(x) = a^x$에서 실수 $a$의 값에 따라 다음과 같다.
(i) $0 < a < 1$일 때
$x$좌표가 3일 때, $y$좌표는 1, 2, 3, 4
$0 < a < 1$이면 $-2 \leq x \leq 1$에서 함수 $f(x) = a^x$은 $x = -2$일 때 최
댓값 3을 가지므로
따라서 구하는 점의 개수는
$5 + 5 + 4 = 14$
$f(-2) = a^{-2} = \frac{1}{a^2} = 3$
$\therefore 14$
$a^2 = \frac{1}{3}$
단계
채점 기준
비율
$\therefore a = \frac{\sqrt{3}}{3}$
0 함수 $y = f(x)$의 그래프를 그린 경우
40%
$f(1), f(2), f(3)$의 값을 각각 구한 경우
30%
$f(x)$ 조건을 만족시키는 점의 개수를 구한 경우
30%
이때 $0 < a < 1$이므로 $a = \frac{\sqrt{3}}{3}$ $\cdots\cdots$①
(ii) $a > 1$일 때
$a > 1$이면 $-2 \leq x \leq 1$에서 함수 $f(x) = a^x$은 $x = 1$일 때 최댓값 3
04 $x = 1$이 방정식 $3^{x+1} = (a+1)^{2x}$의 근이므로
$3^2 = (a+1)^2$
을 가지므로
$a^2 + 2a - 8 = 0$
$f(1) = a = 3$
$(a+4)(a-2) = 0$
이때 $a = 3$은 $a > 1$을 만족시킨다. $\cdots\cdots$②
이때 $a > 0$이므로 $a = 2$ $\cdots\cdots$①
(i), (ii)에서 $a = \frac{\sqrt{3}}{3}$ 또는 $a = 3$이다.
$a = 2$를 방정식 $3^{x+1} = (a+1)^{2x}$에 대입하면
따라서 모든 실수 $a$의 값의 곱은
$3^{x+1} = 3^{2x}$
$\frac{\sqrt{3}}{3} \times 3 = \sqrt{3}$
양변의 밑이 모두 3이므로
$x + 1 = 2x^2$
$\therefore \sqrt{3}$
$2x^2 - x - 1 = 0$
$(2x+1)(x-1) = 0$
단계
채점 기준
비율
$x = -\frac{1}{2}$ 또는 $x = 1$ $\cdots\cdots$②
0 $0 < a < 1$일 때 $a$의 값을 구한 경우
40%
이때 $k \neq 1$이므로 $k = -\frac{1}{2}$
2 $a > 1$일 때 $a$의 값을 구한 경우
40%
따라서 $a + k = 2 + (-\frac{1}{2}) = \frac{3}{2}$ $\cdots\cdots$③
3 모든 실수 $a$의 값의 곱을 구한 경우
20%
$\therefore \frac{3}{2}$

03 함수 $y = \log_2 x$의 그래프를 $y$축에 대하여 대칭이동한 그래프를
나타내는 식은
단계
채점 기준
비율
$y = \log_2 (-x)$
0 양수 $a$의 값을 구한 경우
40%
이 함수의 그래프를 $x$축의 방향으로 4만큼, $y$축의 방향으로 4만큼 평
행이동한 그래프를 나타내는 식은
2 주어진 방정식의 해를 구한 경우
40%
$y = \log_2 (-(x-4)) + 4$, 즉 $y = \log_2 (4 - x) + 4$
③ $k, a+k$의 값을 각각 구한 경우
20%
따라서 $f(x) = \log_2 (4 - x) + 4$이고, 함수 $y = f(x)$의 그래프는 그림
과 같다.
[그림: 좌표평면 위에 로그함수 그래프]
이때 $1 = \log_2 2 < \log_2 3 < \log_2 4 = 2$이므로
$f(1) = \log_2 3 + 4$에서 $5 < f(1) < 6$ $\cdots\cdots$①
48 올림포스 유형편ㆍ대수
05 $\angle A = 90^\circ$, $\overline{AB} = \sqrt{3} \log_2 x$,
$\overline{AC} = 4 - \log_2 x$이므로 직각삼각형 ABC에서
피타고라스 정리에 의하여
$\overline{BC}^2 = \overline{AB}^2 + \overline{AC}^2$
$= (\sqrt{3} \log_2 x)^2 + (4 - \log_2 x)^2$
$= 4 (\log_2 x)^2 - 8 \log_2 x + 16$
$= 4 (\log_2 x - 1)^2 + 12$
$\overline{BC} > 0$에서 $\overline{BC} = \sqrt{4 (\log_2 x - 1)^2 + 12}$ $\cdots\cdots$①
[그림: 직각삼각형 ABC]
