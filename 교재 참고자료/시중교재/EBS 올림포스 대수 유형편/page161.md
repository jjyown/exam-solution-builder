---
book: EBS 올림포스 대수 유형편
page: 161
ocrModel: gemini-2.0-flash
unit: 시중교재
type: EBS 올림포스 대수 유형편
difficulty: 미분류난이도
sourceImage: EBS 올림포스 대수 유형편/pages/page161.png
---

## OCR_본문

이때 함수 f(x)의 치역이 {y|2≤y≤M}이므로 함수 f(x)의 최솟값
은 2이고, 최댓값은 M이다.
양수 $a$의 값의 범위에 따라 나누어 생각하면 다음과 같다.
(i) $0<a<1$일 때
$0<a<1$이면 $g(x)$가 최대일 때, 함수 $f(x)$가 최소이다.
즉, $x=0$일 때 함수 $f(x)$가 최솟값 2를 가지므로
$f(0)=a^{\frac{5}{2}}=2$
$a=2^{\frac{2}{5}}=\sqrt[5]{4}=\sqrt[5]{2^2}$
이때 $\sqrt{2}>1$이므로 $a=\sqrt{2}$는 $0<a<1$을 만족시키지 않는다.
(ii) $a>1$일 때
$a>1$이면 $g(x)$가 최소일 때 함수 $f(x)$가 최소이다.
즉, $x=2$일 때 함수 $f(x)$가 최솟값 2를 가지므로
$f(2)=a^{\frac{-1}{2}}=2$
이때 $a=2^{-2}=\frac{1}{4}$는 $a>1$을 만족시킨다.
(i), (ii)에서 $a=2$
함수 $f(x)$는 $g(x)$가 최대일 때, 즉 $x=0$일 때 최댓값을 가지므로
$M=f(0)=2^5=32$
따라서 $\frac{M}{a}=\frac{32}{2}=16$

22 $f(x)=4^{x+1}-2^{x+1}+1$
$=4\times (2^x)^2-2\times 2^x+1$
$2^x=t$ ($t>0$)이라 하면
$f(x)=4t^2-2t+1=4(t-\frac{1}{4})^2+\frac{3}{4}$
이때 $-3≤x≤0$에서
$2^{-3}≤2^x≤2^0$
즉, $\frac{1}{8}≤t≤1$이므로 함수 $f(x)$는 $t=\frac{1}{4}$일 때 최솟값 $m$을 갖고,
$t=1$일 때 최댓값 $M$을 갖는다.
따라서 $m=\frac{3}{4}$이고 $M=4-2+1=3$이므로
$M-m=3-\frac{3}{4}=\frac{9}{4}$

참고
$t=2^x$이므로
$t=\frac{1}{4}$에서 $2^x=2^{-2}$, $x=-2$
$t=1$에서 $2^x=1$, $x=0$
따라서 함수 $f(x)$는 $x=-2$일 때 최솟값 $\frac{3}{4}$을 갖고, $x=0$일 때 최댓
값 3을 갖는다.

23 $2^x>0$, $\frac{1}{2^x}>0$이므로 산술평균과 기하평균의 관계에 의하여
$f(x)=2^x+\frac{1}{2^x}+2≥2\sqrt{2^x\times\frac{1}{2^x}}+2=4$
(단, 등호는 $2^x=\frac{1}{2^x}$, 즉 $x=0$일 때 성립한다.)
따라서 함수 $f(x)$는 $x=0$일 때 최솟값 4를 가지므로
$a=0$, $m=4$이고
$a+m=0+4=4$

24 $2^x>0$, $2^{-x}>0$이므로 산술평균과 기하평균의 관계에 의하여
$2^{2x}+2^{-2x}≥2\sqrt{2^{2x}\times2^{-2x}}=2$
(단, 등호는 $2^{2x}=2^{-2x}$, 즉 $x=0$일 때 성립한다.)
이때 $2^{2x}+2^{-2x}=t$ $(t≥2)$라 하면
$4^{2x}+4^{-2x}=(2^{2x})^2+(2^{-2x})^2$
$=(2^{2x}+2^{-2x})^2-2$
$=t^2-2$
이므로 함수 $f(x)=(4^{2x}+4^{-2x})+6(2^{2x}+2^{-2x})+3$에서
$f(x)=(t^2-2)+6t+3$
$=(t+3)^2-8$
$t≥2$이므로 함수 $f(x)$는 $t=2$, 즉 $x=0$일 때 최솟값
$f(0)=(2+3)^2-8=17$을 갖는다.

25 ① 함수 $f(x)=log_a x$ ($a>0$, $a≠1$)의 정의역은 양의 실수 전
체의 집합이고, 치역은 실수 전체의 집합이다.
② 함수 $f(x)$는 양의 실수 전체의 집합에서 실수 전체의 집합으로의
일대일대응이므로 $x_1≠x_2$이면 $f(x_1)≠f(x_2)$이다.
즉, $f(x_1)=f(x_2)$이면 $x_1=x_2$이다.
③ $a>1$이면 함수 $f(x)$는 $x$의 값이 증가하면 $f(x)$의 값도 증가한다.
이때 $2<3$이므로 $f(2) <f(3)$이다.
④ 곡선 $y=f(x)$의 점근선은 $y$축, 즉 직선 $x=0$이다.
⑤ $f(1)=log_a 1=0$이므로 곡선 $y=f(x)$는 점 (1, 0)을 지난다.
따라서 옳지 않은 것은 ①이다.

26 로그함수 $y=log_a (\frac{-a^2+8a-3}{3}) x$가 $x$의 값이 증가할 때 $y$의 값도 증
가하려면 로그의 밑이 1보다 커야 하므로
$\frac{-a^2+8a-3}{3}>1$
$-a^2+8a-12>0$
$a^2-8a+12<0$
$(a-2)(a-6)<0$
$2<a<6$
따라서 자연수 $a$의 값은 3, 4, 5이므로 그 개수는 3이다.

27 $f(x)=loga x$ ($a>0$, $a≠1$)에 대하여 곡선 $y=f(x)$가 직선
$y=x-2$와 만나는 점의 개수는 $a$의 값에 따라 다음과 같다.
(i) $0<a<1$일 때
[그림: y=log_a(x) 와 y=x-2 그래프, 4사분면에서 교차]
그림과 같이 곡선 $y=f(x)$는 점 (1, 0)을 지
나고 $x$의 값이 증가하면 $f(x)$의 값은 감소하
므로 점 (2, 0)을 지나고 기울기가 1인 직선
$y=x-2$와 제4사분면 위의 점에서만 만난다.
(ii) $a>1$일 때
[그림: y=log_a(x) 와 y=x-2 그래프, 1,4사분면에서 교차]
그림과 같이 곡선 $y=f(x)$는 점 (1, 0)을 지
나고 $x$의 값이 증가하면 $f(x)$의 값도 증가하
므로 점 (2, 0)을 지나고 기울기가 1인 직선
$y=x-2$와 제1사분면 위의 한 점과 제4사분
면 위의 한 점에서 만난다.
(i), (ii)에서 $a>1$
이때 (ii)의 그림에서 $f(2)>0$이므로 $|f(2)|=2$에서
