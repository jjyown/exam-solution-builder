---
unit: 확률과 통계
type: 1. 여러가지 순열
difficulty: 미분류난이도
sourceImage: 확률과 통계/1. 여러가지 순열#page=69
---

## OCR_본문

수학영역
69
$$\begin{array}{l}
f(2) \geq \sqrt{2}>1 \\
f(3) \geq \sqrt{3}>1 \\
f(4) \geq \sqrt{4}=2 \\
f(5) \geq \sqrt{5}>2
\end{array}$$

이고 조건 (나)에 의하여 치역으로 가능한 경우는 $\{1,2,3\}$, $\{1,2,4\},\{1,3,4\},\{2,3,4\}$
이다.
( i ) 치역이 $\{1,2,3\}$ 인 경우
$f(1)=1, f(5)=3$ 이므로 $\{2,3,4\}$ 에서 $\{2,3\}$ 으로의 함수 중에서 치역이 $\{3\}$ 인 함수를 제외하면 되므로 조건을 만족시키는 함수의 개수는
$${ }_{2} \Pi_{3}-1=2^{3}-1=7$$
(ii) 치역이 $\{1,2,4\}$ 인 경우
(i)의 경우와 마찬가지로 조건을 만족시키는 함수의 개수는 7 이다.
(iii) 치역이 $\{1,3,4\}$ 인 경우 $f(1)=1$ 이므로 $\{2,3,4,5\}$ 에서 $\{3,4\}$ 로의 함수 중에서 치역이 $\{3\},\{4\}$ 인 함수를 제외하면 되므로 조건을 만족시키는 함수의 개수는
$${ }_{2} \Pi_{4}-2=2^{4}-2=14$$
(iv) 치역이 $\{2,3,4\}$ 인 경우
((iv)-(1)) $f(5)=3$ 인 경우
$\{1,2,3,4\}$ 에서 $\{2,3,4\}$ 로의 함수 중에서 치역이 $\{2\},\{3\},\{4\},\{2,3\},\{3,4\}$ 인 함수를 제외하면

되므로 조건을 만족시키는 함수의 개수는
$$\begin{array}{l}
{ }_{3} \Pi_{4}-\left\{3+\left({ }_{2} \Pi_{4}-2\right) \times 2\right\} \\
=3^{4}-\left\{3+\left(2^{4}-2\right) \times 2\right\} \\
=81-31 \\
=50
\end{array}$$
((iv)-(2)) $f(5)=4$ 인 경우
((iv)-(1))의 경우와 마찬가지로 조건을 만족시키는 함수의 개수는 50 이다.
(i), (ii), (iii), (iv)에 의하여 구하는 함수 $f$ 의 개수는 $7+7+14+50 \times 2=128$
32) [정답] (5)
[해설]
공역 $X$ 의 원소 중에서 홀수는 $1,3,5$ 의 세 개다.
조건 (가)에서 $f(1), f(3), f(5)$ 는 모두 홀수다.
$f(1), f(3), f(5)$ 중에서 서로 다른 홀수의 개수에 따라 분류하면
( i ) $f(1), f(3), f(5)$ 가 세 개의 서로 다른 홀수인 경우 $f(1), f(3), f(5)$ 를 정하는 경우의 수는 $\quad{ }_{3} \mathrm{P}_{3}=6$

조건 (다)에 의해 $f(2), f(4)$ 도 모두 홀수다.
조건 (나)에서 3 개의 홀수 중에서 2 개를 선택하면
되므로 $\quad{ }_{3} \mathrm{C}_{2}=3$
$$\therefore 6 \times 3=18$$
(ii) $f(1), f(3), f(5)$ 가 두 개의 서로 다른 홀수인 경우 $f(1), f(3), f(5)$ 를 정하는 경우의 수는
$${ }_{3} C_{2} \times\left(2^{3}-2\right)=18$$

조건 (나), (다)에서 $f(1), f(3), f(5)$ 로 대응된 수 중 하나와 $f(1), f(3), f(5)$ 로 대응된 수를 제외한 세 수 중에서 하나를 골라서 $f(2), f(4)$ 를 대응시키면 되므로
$$\begin{array}{l}
{ }_{2} \mathrm{C}_{1} \times{ }_{3} \mathrm{C}_{1}=6 \\
\therefore 18 \times 6=108
\end{array}$$
(iii) $f(1), f(3), f(5)$ 가 모두 하나의 홀수인 경우 $f(1), f(3), f(5)$ 를 정하는 경우의 수는 $\quad{ }_{3} \mathrm{C}_{1}=3$ 조건 (나), (다)에서 $f(1), f(3), f(5)$ 로 대응된 수를 제외한 네 수 중에서 두 수를 골라서 $f(2), f(4)$ 를 대응시키면 되므로 $\quad{ }_{4} \mathrm{C}_{2}=6$
$$\therefore 3 \times 6=18$$

이상에서 구하는 경우의 수는
$$18+108+18=144$$
33) [정답] 260
[해설]
조건 (다)에서 함수 $f$ 는 상수함수일 수 없으므로 $n(A)=2$ 또는 $n(A)=3$
(i) $n(A)=2$ 인 경우

집합 $A$ 를 정하는 경우의 수는 ${ }_{5} \mathrm{C}_{2}=10$
$A=\{1,2\}$ 인 경우를 생각하면 조건 (다)에서
$f(1)=2, f(2)=1$
$f(3), f(4), f(5)$ 의 값은 1,2 중 하나이므로
$f(3), f(4), f(5)$ 의 값을 정하는 경우의 수는
${ }_{2} \Pi_{3}=2^{3}=8$
즉, $n(A)=2$ 인 경우 함수 $f$ 의 개수는
$10 \times 8=80$
(ii) $n(A)=3$ 인 경우

집합 $A$ 를 정하는 경우의 수는 ${ }_{5} \mathrm{C}_{3}=10$
$A=\{1,2,3\}$ 인 경우를 생각하면 조건 (다)에서
순서쌍 $(f(1), f(2), f(3))$ 은 $(2,3,1),(3,1,2)$ 뿐이므로
$f(1), f(2), f(3)$ 의 값을 정하는 경우의 수는 2
$f(4), f(5)$ 의 값은 $1,2,3$ 중 하나이므로 $f(4), f(5)$ 의
값을 정하는 경우의 수

69
91
